{
  "name": "pf2e-aura-forge",
  "version": "1.0.0-rc.2",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "release:check": "npm test && node tools/release-check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
